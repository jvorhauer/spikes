akka {
  extensions = [akka.persistence.Persistence]

  log-dead-letters = 10
  log-dead-letters-during-shutdown = on
  log-config-on-start = off
  loggers = ["akka.event.Logging$DefaultLogger"]
  loglevel = INFO
  stdout-loglevel = INFO

  actor {
    serializers {
      jackson-cbor = "akka.serialization.jackson.JacksonCborSerializer"
    }
    serialization-bindings {
      "spikes.model.package$Command" = jackson-cbor
      "spikes.model.package$Entity" = jackson-cbor
      "spikes.model.package$Event" = jackson-cbor
      "spikes.model.Users" = jackson-cbor
      "spikes.model.State" = jackson-cbor
    }
  }

  persistence {
    journal {
      plugin = "akka.persistence.cassandra.journal"
      auto-start-journals = ["akka.persistence.cassandra.journal"]
    }
    cassandra {
      session-provider = "akka.stream.alpakka.cassandra.DefaultSessionProvider"
      session-dispatcher = "akka.persistence.cassandra.default-dispatcher"
      datastax-java-driver-config = "datastax-java-driver"
    }
    snapshot-store {
      plugin = "akka.persistence.snapshot-store.local"
      auto-start-snapshot-stores = ["akka.persistence.snapshot-store.local"]
    }
  }
}

datastax-java-driver {
  basic {
    session-keyspace = "akka"
    cloud {
      secure-connect-bundle = ${ASTRA_SCB}
    }
  }
  advanced {
    auth-provider {
      class = PlainTextAuthProvider
      username = ${ASTRA_USERNAME}
      password = ${ASTRA_PASSWORD}
    }
    reconnect-on-init = true
  }
}

kamon {
  environment.service = "Spikes"
  apm.api-key = ${KAMON_TOKEN}
}
